rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser access is blocked; Cloud Run uses IAM through the API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
